# AgentLoom Backend

FastAPI application for AgentLoom. The current foundation exposes a health endpoint and provides typed environment configuration plus JSON application logs.

```bash
uv sync
uv run uvicorn agentloom.main:app --reload
```

